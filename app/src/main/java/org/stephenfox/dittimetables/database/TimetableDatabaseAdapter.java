package org.stephenfox.dittimetables.database;

import org.stephenfox.dittimetables.timetable.TimetableAdapter;
import org.stephenfox.dittimetables.timetable.TimetableSession;

public class TimetableDatabaseAdapter implements TimetableAdapter {

  
  @Override
  public TimetableSession getSession() {
    return null;
  }
}
